apply plugin: 'android'

android {
    compileSdkVersion 19
    buildToolsVersion '19.1.0'
    defaultConfig {
        packageName "com.beepscore.crazytipcalc.app"
        minSdkVersion 18
        targetSdkVersion 19
        versionCode 1
        versionName '1.0'
        applicationId 'com.beepscore.crazytipcalc.app'
    }
    buildTypes {
        release {
            runProguard false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    productFlavors {
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
}
